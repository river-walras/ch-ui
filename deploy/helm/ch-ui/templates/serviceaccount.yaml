{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ch-ui.serviceAccountName" . }}
  labels:
    {{- include "ch-ui.labels" . | nindent 4 }}
{{- end }}
